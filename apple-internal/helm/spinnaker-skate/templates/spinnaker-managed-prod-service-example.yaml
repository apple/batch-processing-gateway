{{ if false }}
apiVersion: v1
kind: Service
metadata:
  name: prod
  namespace: UPDATE-ON-MANUAL-SPINNAKER-DEPLOYMENT-WITH-PROD-NAMESPACE
spec:
  type: ClusterIP
  selector:
    prod: 'true'
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
{{ end }}
