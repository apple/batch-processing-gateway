{{- if .Values.notary.notaryAacEnabled }}
apiVersion: v1
kind: Service
metadata:
  name: skate-aac-authed
  namespace: {{ .Values.skate.namespace }}
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9090
  selector:
    app: skate-aac
{{- end }}
