{{ if false }}
apiVersion: v1
kind: Service
metadata:
  name: stage-auth
  namespace: UPDATE-ON-MANUAL-SPINNAKER-DEPLOYMENT-WITH-STAGE-NAMESPACE
spec:
  type: ClusterIP
  selector:
    stageAuth: 'true'
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9090
{{ end }}
